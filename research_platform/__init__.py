"""Research Platform v2 — archive index + bot delivery.

Package tên `research_platform` (KHÔNG dùng `platform` — trùng module stdlib Python,
Pyrogram import lỗi: AttributeError python_implementation).
"""
