================================================================================
PROMPT DỰ ÁN — UpBain Research Platform v2
Lộ trình: MÓNG → KHUNG → VỮNG (từng phase, không làm cả dự án một lần)
================================================================================

CÁCH DÙNG PROMPT NÀY
--------------------
  - Mỗi lần giao AI CHỈ 1 PHASE (hoặc 1 bước trong phase).
  - Phase sau CHỈ bắt đầu khi checklist phase trước PASS.
  - Hai repo bên dưới chỉ tham chiếu ý tưởng — không copy cả dự án.
  - Nhánh research-bot-platform KHÔNG phải repo mẫu — chỉ nơi implement.

  HAI REPO THAM CHIẾU:
    UpBain  → cursor/web-bot-schedule-realtime-fe96
              (userbot, web, flood, lịch, topic map)
    Clender → cursor/fix-web-backup-access-2234
              (membership, delivery fallback, backup, deep link)

  Clone tham khảo:
    git clone -b cursor/web-bot-schedule-realtime-fe96 \
      https://github.com/easlymazzawi-collab/upbain /tmp/upbain-ref
    git clone -b cursor/fix-web-backup-access-2234 \
      https://github.com/easlymazzawi-collab/clender /tmp/clender-ref

================================================================================
NGỮ CẢNH DỰ ÁN — AI MỚI ĐỌC PHẦN NÀY TRƯỚC
================================================================================

  BỐI CẢNH (từ quá trình làm việc thực tế):

  Chủ dự án vận hành kênh research Telegram: userbot up bài lên nhiều kênh,
  user end-user xem lại archive theo ngày qua bot, có VIP/Stars, share đua top,
  quản lý ads alias, backup dữ liệu.

  Đã có 2 tool chạy được riêng:
    (1) UpBain web-bot — mạnh userbot auto-up, web điều khiển, lịch, flood fix
    (2) Clender — mạnh bot chia sẻ link, membership, delivery media, backup ZIP,
        web admin sidebar đẹp

  Dự án MỚI = Research Platform: GỘP TƯ DUY vận hành, KHÔNG dán 2 repo.
  AI trước hay mắc lỗi:
    - Copy nguyên code 2 dự án → lộn xộn, UI xấu, tab chồng tab index.html v28
    - Làm cả dự án một prompt → không xong, không test
    - Nhầm nhánh research-bot-platform làm "repo mẫu" (đó chỉ nhánh implement)

  CÁCH LÀM VIỆC CỦA CHỦ DỰ ÁN:
    - Giao từng phase (móng → khung → web UI → xương → vững)
    - Mỗi phase verify checklist rồi mới phase tiếp
    - Repo 1 & 2 chỉ để HỎI: "bài toán này họ giải thế nào?" — rồi viết lại
    - Web Platform admin = app sidebar riêng (học UX clender), không vá tab cũ

  DỰ ÁN SẮP CODE LÀ GÌ (một câu):
  "Userbot UpBain up kênh + ghi index link ngày VN + bot aiogram trả user +
   web admin Research Platform + monetize VIP/Stars — archive chỉ metadata (A2)."

  KHÁC Clender: archive theo NGÀY + userbot + 10 nguồn, không phải share album lẻ.
  KHÁC UpBain thuần: thêm lớp bot delivery, VIP, index DB, không chỉ up kênh.

================================================================================
#1 CLENDER — cursor/fix-web-backup-access-2234
Điểm mạnh & thư viện (CHỌN LỌC áp dụng, không copy cả repo)
================================================================================

  URL: https://github.com/easlymazzawi-collab/clender/tree/cursor/fix-web-backup-access-2234

  THƯ VIỆN CHÍNH:
    python-telegram-bot[job-queue] 21.6   Bot API async (ApplicationBuilder)
    telethon 1.36 + cryptg 0.4            Userbot clone forum (forwarder)
    flask 3.0 + gunicorn                    Web admin + REST (dự án mới dùng FastAPI)
    python-dotenv, aiohttp                Config .env, webhook
    sqlite3 (stdlib) WAL + PRAGMA          DB nhẹ, check_same_thread=False

  ĐIỂM MẠNH THEO MODULE:

  bot/handlers.py
    - Deep link /start TOKEN → serve album (copy_messages batch)
    - Fallback 3 tầng: copy → forward batch → từng msg
    - FloodWait RetryAfter sleep + retry (3 lần)
    - Album buffer media_group_id (gom 1 link)
    - Admin panel inline keyboard (/panel)
    - Broadcast copy_message tới user list
    - Link config: expires, max_views, allow_forward (callback cfg:)

  bot/membership.py
    - Force-join kênh trước mọi handler (gate)
    - Cache TTL: member 300s, non-member 60s
    - Fail-open khi bot không vào được kênh (tránh khóa all user)
    - Pending token sau join (set_pending_token / pop_pending_token)
    - Nút Join + callback check_membership

  utils/token.py
    - generate_numeric_token(16) — deep link số kiểu file-share bot
    - generate_token alphanumeric URL-safe

  utils/backup.py
    - SQLite backup API (src.backup(dst)) — an toàn khi đang ghi
    - ZIP rotate theo KEEP_DAYS
    - send_backup_to_telegram → topic "backup" forum đích
    - start_backup_scheduler thread nền + run_backup_now()
    - get_backup_status() cho web health/API

  database/models.py
    - media_albums: src_chat_id + src_msg_ids JSON (mẫu TƯ DUY index link)
    - WAL journal, row_factory Row
    - bot_settings key/value
    - validate_album_access, increment_album_access

  forwarder/core.py + runner.py
    - Telethon batch forward 50 msg/call, album grouped_id
    - Resume state file + SQLite mirror
    - Progress callback cho web SSE
    - parse_link t.me/c/... forum private

  forwarder/link_sender.py
    - Preview + caption khi forward (premium emoji entities)

  web/ (UX tham khảo — viết lại FastAPI)
    - templates/base.html: sidebar trái + topbar + footer
    - static/css/style.css: Inter font, dark theme, CSS variables, card/table
    - Dashboard tách section: Forwarder | Bot Link | Hệ thống

  app.py + run.py
    - GET/POST /api/backup status + run
    - Launcher bot + web + backup scheduler + web watchdog restart
    - deploy/: Windows bat, systemd service, ubuntu setup

  KHÔNG LẤY từ Clender:
    Domain share album lẻ làm sản phẩm chính, Flask stack, schema media_links,
    upload whitelist, Telethon forwarder làm core Research Platform.

================================================================================
#2 UPBAIN — cursor/web-bot-schedule-realtime-fe96
Điểm mạnh & thư viện (NỀN userbot + web — áp dụng phase 0–2)
================================================================================

  URL: https://github.com/easlymazzawi-collab/upbain/tree/cursor/web-bot-schedule-realtime-fe96

  THƯ VIỆN CHÍNH:
    pyrogram >= 2.0 + tgcrypto              Userbot MTProto nhanh
    fastapi + uvicorn + pydantic            Web API + validate body
    (phase sau) aiogram 3.x                 Bot delivery — thêm khi phase 2

  Cấu hình: data/auto_config.json (KHÔNG .env bắt buộc) — web là nguồn config.

  ĐIỂM MẠNH THEO MODULE:

  tool__tauto_nostage.py
    - Entry: userbot + web server + nhiều background task asyncio
    - Lệnh /runtopic, /scan, /allrun, /help
    - Pipeline lock chống chạy song song
    - Message cache giảm GetMessages flood

  core/auto_runner.py
    - run_topic_batch: collect → xếp → forward → pin/inventory
    - Nhánh ads vs plain (Up bài) tách branch
    - Notify HTML qua bot token (link msg, trạng thái)
    - Tích hợp up_confirm, stock_watcher, map_limits

  core/source_collector.py + topic_parser.py
    - Collect batch từ forum topic (cursor/start_msg)
    - Parse caption "30 media 2 ads"
    - posts_to_content_refs, album grouping

  core/saved_staging.py
    - Stage batch → Saved Messages 1 lần → up kênh từ Saved (giảm flood nguồn)

  core/scheduler.py
    - Lịch auto hằng ngày HH:MM, timezone VN
    - compute_next_run_ts, scheduler_loop 20s tick
    - /all chạy sau hết topic (commit e14d4e8+ trên nhánh)

  core/config_store.py
    - topic_sources, plain_topic_sources, all_task, global, schedule
    - topic_key(src, topic_id), upsert_topic_source
    - Thread lock JSON read/write

  core/channel_store.py + map_config.py + map_limits.py
    - channels.json, topic_map.txt, RR round-robin
    - Limit posts/media per cmd mapping
    - @xepbai mode vs content/ads ratio (normal/xdone/zdone)

  core/inventory.py + stock_watcher.py
    - Kho media topic, cảnh báo kho thấp
    - require_full_batch trước khi up

  core/pin_manager.py + pin_bot.py
    - Advance pin bài kế trên nguồn (cursor cho lượt sau)
    - Pin bot token riêng hoặc dùng notify bot

  core/up_confirm.py
    - Xác nhận /upngay trước up; hủy pending gần giờ lịch

  core/forum_topic.py
    - Pyrogram 2.0 forum topic API workaround (iter_topic_history)

  core/import_legacy.py
    - Import ZIP/.env/topic_map/session → auto_config (migration tool cũ)

  core/web_actions.py + runtime.py
    - Queue lệnh web → userbot (run_full_cycle, run_topic…)
    - SSE log real-time trên dashboard

  core/bot_notify.py
    - Gửi notify qua Bot API (HTML) không cần userbot

  web/server.py + static/index.html
    - FastAPI auth token header
    - Dashboard v28: START/STOP, topic map, kho, lịch, import
    - (Giữ cho userbot — Research admin tách Phase W)

  KHÔNG LẤY nguyên xi:
    Layout tab web v28 cho Platform admin, thiếu bot multi-delivery, thiếu archive index.

================================================================================
BẢNG ÁP DỤNG: ĐIỂM MẠNH → PHASE DỰ ÁN MỚI
================================================================================

  Điểm mạnh (nguồn)                    Phase    Dùng để làm gì
  ------------------------------------  -------  ----------------------------------
  UpBain entry + web SSE                0        Tool chạy, config web
  UpBain auto_runner + scheduler        0–2      L1 up kênh, lịch auto
  UpBain saved_staging + flood serial   0–2      Giảm flood khi up
  UpBain import_legacy                  0,5      Migrate tool cũ
  UpBain web_actions queue              0        START từ web
  (thiết kế mới) SQLite archive ngày    1–2      L2 index — KHÔNG schema clender
  Clender web sidebar UX                W        Admin Platform UI
  UpBain bot_notify pattern             2,5      Admin notify group
  Clender _serve_album fallback         3        Bot delivery vững
  Clender membership gate               3        Force-join
  Clender backup.py pattern             3,5      ZIP + scheduler + Telegram
  Clender numeric token                 4c       Share deep link
  (thiết kế mới) multi-bot queue        4a       10 bot token
  (thiết kế mới) VIP/Stars              4b       Monetize
  UpBain ads/xepbai + alias mới         4c       Ads trong index ngày
  (thiết kế mới) rollup 30 ngày         5        Catalog text

  Nguyên tắc ô "thiết kế mới": học ý UpBain/Clender nhưng schema & flow archive
  ngày là spec riêng — không ép fit media_albums hay topic_map thuần.

================================================================================
STACK DỰ ÁN MỚI (tổng hợp — không trùng 100% repo nào)
================================================================================

  Userbot:     Pyrogram + tgcrypto          ← từ UpBain
  Bot user:    aiogram 3.x                  ← thêm phase 2 (Clender dùng python-telegram-bot)
  Web userbot: FastAPI + uvicorn + SSE     ← từ UpBain
  Web platform: static sidebar app         ← UX học Clender, stack FastAPI
  DB:          SQLite WAL platform.db       ← pattern Clender + schema mới
  Config:      auto_config.json platform.*  ← từ UpBain style
  Timezone:    Asia/Ho_Chi_Minh             ← chung

  Lưu ý: Clender Bot API ≠ aiogram — khi port delivery/membership đổi API tương ứng.

================================================================================


  SPEC cố định (không đổi giữa chừng):
    A1  Không xóa ads trong chat user; strip khi xem archive sau revoke
    A2  Archive = link + metadata; KHÔNG forward vào forum backup
    B4  Tối đa 10 bot token (mở rộng dần, phase đầu chỉ 1 bot)
    B6  1 userbot + web chung
    C9  Ngày VN label 30-06-2026, timezone Asia/Ho_Chi_Minh

  CẤM mọi phase:
    - Làm hết VIP + 10 bot + rollup + backup Telegram trong 1 prompt
    - Copy nguyên folder clender hoặc cả repo web-bot sang
    - Folder tên platform/ (crash Windows Pyrogram)
    - Forward media vào forum backup

  Mỗi phase xong: commit nhỏ, mô tả rõ phase, chạy verify checklist.

================================================================================
TỔNG QUAN LỘ TRÌNH
================================================================================

  Phase 0  MÓNG      Nền chạy được: userbot + web + config + log
  Phase 1  KHUNG     Schema DB + 3 lớp ON/OFF + hook rỗng + API platform
  Phase W  WEB UI    Admin Research Platform — sidebar mới (KHÔNG vá tab cũ)
  Phase 2  XƯƠNG     L1 up kênh + L2 ghi index + L3 bot /day 1 nguồn
  Phase 3  CƠ BẢN    Delivery vững (clender) + membership + backup local
  Phase 4  MỞ RỘNG  Multi-bot queue + VIP/Stars + ads alias + share
  Phase 5  VỮNG      Rollup + backup Telegram + notify admin + hardening

  !!! AI chỉ nhận task của 1 phase. Không nhảy phase. !!!

================================================================================
WEB UI — QUY TẮC (AI HAY LÀM XẤU — ĐỌC KỸ)
================================================================================

  VẤN ĐỀ:
  AI thường "thêm tab Research Platform" vào web/static/index.html (UpBain v28)
  → file 2500+ dòng, tab chồng tab, sub-tab lộn xộn, UX xấu, khó sửa mãi.

  QUY TẮC CẤM:
  x Thêm sub-tab / form mới vào section #platform trong index.html v28
  x Copy thêm 500 dòng JS inline vào index.html cho Platform
  x Giữ layout "tabs ngang trên cùng" cho admin Research Platform
  x Bảo "UI tạm" rồi không bao giờ làm lại

  QUY TẮC BẮT BUỘC:
  ✓ Research Platform admin = WEB APP RIÊNG (route + file riêng)
  ✓ Layout sidebar trái (học UX clender base.html + style.css — viết lại cho FastAPI)
  ✓ Font Inter, dark theme, card + table gọn, mobile có menu toggle
  ✓ API REST giữ /api/platform/* — chỉ đổi frontend
  ✓ Dashboard userbot v28 (index.html) giữ nguyên cho: START, topic map, lịch, kho
  ✓ Link chéo: nút "Research Admin →" từ dashboard cũ; "← Userbot" từ admin mới

  CẤU TRÚC FILE GỢI Ý (Phase W):

    web/
      static/
        index.html              ← userbot dashboard (CHẠM TỐI THIỂU)
        platform/
          index.html            ← shell sidebar (hoặc FastAPI TemplateResponse)
          css/app.css           ← design system riêng (copy ý clender, không paste Flask)
          js/app.js             ← fetch /api/platform/*, routing sidebar
      server.py                 ← GET /admin/platform, /admin/platform/{page}

  SIDEBAR Research Platform (mục menu):

    Tổng quan      — trạng thái bot, queue, lượt hôm nay
    Cấu hình       — ON/OFF 3 lớp, forum IDs, membership, backup
    Bots           — CRUD token, restart
    Archive        — calendar/list ngày, publish/close
    VIP & Stars    — plans, grant
    Giftcode
    Quảng cáo      — alias, recheck
    Share          — leaderboard, event
    Users
    Backup         — status, run, download

  Tham chiếu UX (không copy Flask):
    clender/web/templates/base.html  — cấu trúc sidebar + topbar
    clender/web/static/css/style.css — token màu, card, table

  Phase 1–4: chỉ cần API + trang admin tối thiểu (Phase W).
  Phase 5: mọi màn Platform phải nằm trong admin mới, không swell index.html.

================================================================================
PHASE 0 — MÓNG (Foundation)
Mục tiêu: Tool chạy được, web mở được, userbot kết nối Telegram
================================================================================

  GIAO CHO AI (copy prompt phase này):

  ---
  Phase 0 only. Dựng nền UpBain Research Platform.

  Tham chiếu: upbain @ cursor/web-bot-schedule-realtime-fe96
  - tool__tauto_nostage.py entry
  - web/server.py FastAPI + SSE
  - core/config_store.py → data/auto_config.json
  - Cài Pyrogram, chạy web port 8080

  Deliverables:
  1. pip install -r requirements.txt → python tool chạy không crash
  2. Web login/token, tab Telegram & Bot: api_id, api_hash, notify bot
  3. START/STOP userbot, log SSE hiện trên dashboard
  4. Package platform code tên research_platform/ (không platform/)
  5. Windows: bootstrap stdlib platform nếu cần

  CHƯA làm: SQLite platform, bot delivery, VIP, clender port, 10 bot.

  Verify:
  [ ] Web http://127.0.0.1:8080 mở
  [ ] Nhập API → restart → userbot online
  [ ] Auto 1 topic test (hoặc /runtopic) forward được 1 batch
  [ ] Không có folder platform/ shadow stdlib

  Xong → commit "Phase 0: foundation userbot + web"

  ---

  Mượn ý từ web-bot: entry script, web SSE, config JSON, flood serial cơ bản.
  Không copy research-bot branch.

================================================================================
PHASE 1 — KHUNG (Skeleton)
Mục tiêu: Kiến trúc 3 lớp + DB + config platform — chưa cần bot gửi media
================================================================================

  GIAO CHO AI:

  ---
  Phase 1 only. Khung Research Platform.

  Prerequisite: Phase 0 PASS.

  Deliverables:
  1. research_platform/db.py — init_db() tạo bảng:
     bots, days, day_items, users (tối thiểu)
  2. research_platform/config.py — load/save platform.* trong auto_config.json
     Keys: enabled, publish_channels, archive_index, bot_delivery,
           admin_forum_id, delivery_delay_sec, bot { token, source_forum_id... }
  3. research_platform/hooks.py — after_auto_forward() stub (log only)
  4. Hook gọi sau lượt forward userbot (1 dòng trong pipeline hiện có)
  5. Web: CHỈ API GET/PATCH /api/platform — KHÔNG thêm tab Platform vào index.html v28
     (UI admin → Phase W)

  CHƯA làm: bot aiogram chạy thật, copy message, VIP, backup, membership, web UI đẹp.

  Verify:
  [ ] data/platform.db tạo sau init
  [ ] Bật platform.enabled trên web → lưu được
  [ ] Chạy auto 1 lượt → hook log "after_auto_forward" (chưa ghi DB cũng OK)
  [ ] 3 checkbox lớp lưu/đọc đúng (test qua curl hoặc trang tạm — UI đẹp Phase W)

  Xong → commit "Phase 1: platform skeleton DB + config + hook stub"

  ---

  Thiết kế schema theo spec — không copy clender media_albums.

================================================================================
PHASE W — WEB UI (Admin Research Platform — sidebar mới)
Mục tiêu: Giao diện admin đẹp, tách hẳn khỏi dashboard userbot v28
Chạy sau Phase 1 (API đã có) hoặc song song Phase 2 — TRƯỚC Phase 4
================================================================================

  GIAO CHO AI:

  ---
  Phase W only. Web admin Research Platform — KHÔNG sửa index.html v28 (trừ 1 link).

  Prerequisite: Phase 1 PASS (/api/platform hoạt động).

  Tham chiếu UX: clender base.html + style.css (layout sidebar, màu, card).
  Stack: FastAPI serve static hoặc HTMLResponse; fetch API có sẵn; auth token giữ nguyên.

  Deliverables:
  1. Route GET /admin/platform (+ /admin/platform/* optional SPA hash)
  2. web/static/platform/ — app.css, app.js, index.html (sidebar + main content)
  3. Trang tối thiểu hoạt động:
     - Cấu hình (ON/OFF 3 lớp, forum IDs, lưu PATCH)
     - Bots (list + form token)
     - Archive (bảng days + publish/close)
  4. index.html v28: CHỈ thêm 1 nút/link "Research Admin" → /admin/platform
  5. Responsive: sidebar collapse mobile (menu toggle)

  CẤM trong phase này:
  - Thêm plat-sub-tab vào #platform trong index.html
  - Inline thêm 200+ dòng CSS/JS vào index.html

  Verify:
  [ ] /admin/platform mở, sidebar rõ ràng, không giống tab cũ
  [ ] Lưu config Platform từ admin mới → reload đúng
  [ ] index.html v28 diff < 20 dòng (chỉ link)
  [ ] Mobile: sidebar toggle OK

  Xong → commit "Phase W: platform admin sidebar UI (separate from v28 dashboard)"

  Các phase sau (4, 5): thêm màn VIP/Gift/Ads/Share/Users/Backup vào admin MỚI,
  không quay lại index.html.

  ---

================================================================================
PHASE 2 — XƯƠNG (Core pipeline — 1 bot, 1 ngày)
Mục tiêu: Up kênh → ghi index → bot /day gửi được (happy path đơn giản)
================================================================================

  GIAO CHO AI:

  ---
  Phase 2 only. Core pipeline 1 bot 1 ngày.

  Prerequisite: Phase 1 PASS.

  Deliverables:
  1. archive_index.py — sau auto forward:
     - Tạo/update day theo topic_label VN (30-06-2026)
     - Ghi day_items: seq, src_chat_id, src_msg_id, album_msg_ids JSON
  2. dates.py — today_vn(), topic_label_vn(), parse_day_label()
  3. bot_manager.py + bot_delivery.py — start 1 bot aiogram nền
  4. bot_handlers.py tối thiểu:
     - /start, /today, /day DD-MM-YYYY
     - deliver_day_to_user: loop items, copy_message đơn giản
     - delay delivery_delay_sec giữa bài
  4. user_deliveries — lưu last_seq_sent (resume stub)
  5. Web: bot token qua /admin/platform (Phase W) — không form trong index.html cũ
  6. Archive tab: list days — trong admin mới nếu Phase W đã xong

  CHƯA làm: fallback forward, membership, VIP, 10 bot, ads alias, backup.

  Verify:
  [ ] Auto up 1 lượt → DB có 1 day + N day_items
  [ ] Publish day (API hoặc tạm auto published)
  [ ] Bot /today gửi đúng thứ tự seq (happy path, forum nguồn bot đã join)
  [ ] Tắt archive_index → không ghi DB; tắt bot_delivery → bot không gửi

  Xong → commit "Phase 2: index + 1 bot /day happy path"

  ---

  Mượn ý web-bot: hook sau forward.
  Copy message: viết đơn giản trước — fallback để Phase 3.

================================================================================
PHASE 3 — CƠ BẢN VỮNG (Robustness — pattern Clender)
Mục tiêu: Delivery không gãy, membership, backup ZIP local
================================================================================

  GIAO CHO AI:

  ---
  Phase 3 only. Vững delivery + membership + backup local.

  Prerequisite: Phase 2 PASS.

  Tham chiếu ý clender (viết lại, không paste file):
  - _serve_album → deliver_item(): copy→forward→per-msg, FloodWait 3 lần
  - membership.py → cache TTL, fail-open, gate_or_prompt, mem_recheck
  - backup.py → safe sqlite copy, ZIP, rotate local

  Deliverables:
  1. _copy_item / deliver_item fallback đầy đủ (clender thứ tự)
  2. membership.py + config membership_channel_id, force_join_check_sec
  3. backup.py create_backup_file + GET download ZIP
  4. Web: membership + backup trong /admin/platform (Phase W pages)
  5. resume delivery: bot die giữa seq → tiếp từ last_seq_sent

  CHƯA làm: 10 bot queue, VIP Stars, ads, share, Telegram upload backup, rollup.

  Verify:
  [ ] Simulate FloodWait → bot retry, không skip bài
  [ ] copy fail → forward fallback hoạt động
  [ ] User chưa join kênh → bị chặn + recheck OK sau join
  [ ] Backup ZIP có platform.db + auto_config masked + archive_index.json
  [ ] Resume: dừng giữa delivery → /day tiếp từ seq cũ

  Xong → commit "Phase 3: clender patterns delivery + membership + backup local"

  ---

================================================================================
PHASE 4 — MỞ RỘNG (Features)
Mục tiêu: Multi-bot, monetize, ads, share — vẫn từng mảnh nếu cần
================================================================================

  GIAO CHO AI — có thể tách 4a / 4b / 4c:

  ---
  Phase 4a — Multi-bot queue (Prerequisite: Phase 3 PASS)

  Deliverables:
  - bots[] config max 10, queue_order
  - run_queue.py orchestrator tuần tự
  - bot_manager restart đơn/all
  - Web tab Bots (10)

  Verify: 2 bot config → queue chạy lần lượt, restart 1 bot OK.

  ---
  Phase 4b — VIP + Stars + giftcode

  Deliverables:
  - vip_plans, gift_codes tables + web CRUD
  - /vip, /redeem, /buy_{id} invoice XTR
  - can_access_archive nếu require_vip_for_archive

  Verify: grant VIP web → /today OK; Stars test invoice flow.

  ---
  Phase 4c — Ads + Share + Catalog

  Deliverables:
  - ads_contracts, alias, terminate, recheck sau up
  - share_refs numeric token, /invite, leaderboard
  - /find, contribution forward admin

  Verify: revoke alias → xem ngày không còn ads; share 1 user 1 click.

  Xong mỗi sub-phase → commit riêng.

  ---

================================================================================
PHASE 5 — VỮNG (Production-ready)
Mục tiêu: Rollup, backup Telegram, web đủ, notify admin, hardening
================================================================================

  GIAO CHO AI:

  ---
  Phase 5 only. Hoàn thiện vận hành.

  Prerequisite: Phase 4 PASS.

  Deliverables:
  1. rollup.py — 30 ngày text mục lục, không xóa cũ
  2. backup scheduler + send_backup_to_telegram + POST /api/platform/backup
  3. admin_notify_group — tin sau mỗi lượt auto (link kế, trạng thái)
  4. Web sub-pages trong /admin/platform: VIP, Gift, Ads, Share, Users, Backup
     (KHÔNG thêm vào index.html v28)
  5. Publish/Close day, recheck ads button
  6. Docs DISASTER_RECOVERY: bot die, restore ZIP

  Verify:
  [ ] Rollup post catalog topic
  [ ] Backup auto 24h + Backup ngay + file lên Telegram topic
  [ ] End-to-end: lịch auto → index → publish → user /today → share link
  [ ] Import dữ liệu cũ (.env, topic_map) vẫn hoạt động (web-bot nền)

  Xong → commit "Phase 5: production rollup backup notify"

  ---

================================================================================
PROMPT MẪU GIAO AI (template — điền phase)
================================================================================

  Bạn build UpBain Research Platform v2. ĐỌC mục NGỮ CẢNH + BẢNG ÁP DỤNG trước.

  Hai repo tham chiếu (chỉ mượn điểm mạnh — KHÔNG copy cả dự án):
  1. upbain @ cursor/web-bot-schedule-realtime-fe96 — userbot, web, lịch, flood
  2. clender @ cursor/fix-web-backup-access-2234 — delivery, membership, backup, UI

  CHỈ làm Phase [N]. Spec A1/A2. Không vá index.html v28 — dùng Phase W sidebar.

  Output: code + checklist PASS/FAIL. Không làm phase khác.

================================================================================
PHASE 0 — PROMPT NGẮN SẴN COPY
================================================================================

  Phase 0 only. Dựng nền: userbot Pyrogram + FastAPI web + auto_config.json.
  Tham chiếu upbain @ cursor/web-bot-schedule-realtime-fe96 (entry, web SSE,
  config). Package research_platform/ empty stub. Chưa DB platform, chưa bot
  delivery. Verify: web mở, API Telegram, auto 1 batch forward. Commit Phase 0.

================================================================================
PHASE 1 — PROMPT NGẮN SẴN COPY
================================================================================

  Phase 1 only. Khung: SQLite, config platform.*, hook stub, GET/PATCH
  /api/platform. KHÔNG thêm tab Platform vào index.html. UI → Phase W. Commit Phase 1.

================================================================================
PHASE W — PROMPT NGẮN SẴN COPY
================================================================================

  Phase W only. Tạo web admin Research Platform RIÊNG: /admin/platform,
  sidebar ( học layout clender ), web/static/platform/{css,js,index.html}.
  Fetch /api/platform/*. CẤM thêm sub-tab vào index.html v28 — chỉ thêm link
  "Research Admin". Trang: Cấu hình, Bots, Archive. Prerequisite Phase 1 PASS.
  Commit Phase W.

================================================================================
PHASE 2 — PROMPT NGẮN SẴN COPY
================================================================================

  Phase 2 only. Core 1 bot: archive_index ghi sau auto, 1 aiogram bot
  /today /day, copy đơn giản + delay, web bot token + archive list. Prerequisite
  Phase 1 PASS. Chưa fallback clender, chưa VIP. Verify checklist Phase 2.
  Commit Phase 2.

================================================================================
PHASE 3 — PROMPT NGẮN SẴN COPY
================================================================================

  Phase 3 only. Mượn ý clender (viết lại): delivery copy→forward→per-msg +
  FloodWait, membership gate, backup ZIP local + download web. Resume
  last_seq_sent. Prerequisite Phase 2 PASS. Chưa 10 bot/VIP/rollup. Verify
  Phase 3. Commit Phase 3.

================================================================================
PHASE 4a/4b/4c — PROMPT NGẮN (chọn một)
================================================================================

  4a: Multi-bot queue max 10, restart web. Phase 3 PASS.
  4b: VIP plans, Stars XTR, giftcode web+bot. Phase 3 PASS (4a optional song song).
  4c: Ads alias revoke, share ref leaderboard, /find contribution. Phase 3 PASS.

================================================================================
PHASE 5 — PROMPT NGẮN SẴN COPY
================================================================================

  Phase 5 only. Rollup 30 ngày, backup scheduler + Telegram upload, admin
  notify group, web tabs đủ, publish/close day, disaster recovery doc. Prerequisite
  Phase 4 PASS. Verify end-to-end. Commit Phase 5.

================================================================================
BẢNG PHỤ THUỘC PHASE
================================================================================

  Phase 0 ──► Phase 1 ──► Phase W ──► Phase 2 ──► Phase 3 ──► Phase 4a ──► Phase 5
                              │              │
                              │         Phase 4b ──┘
                              │              │
                              └──────── Phase 4c

  Phase W nên xong TRƯỚC khi Phase 4 (nhiều màn web) — tránh AI vá index.html.

  Không được: Phase 0 → Phase 5
  Không được: thêm UI Platform vào index.html thay vì Phase W

================================================================================
KHI AI TRẢ LỜI XONG 1 PHASE — BẠN REVIEW
================================================================================

  1. Chạy checklist verify của phase đó
  2. PASS → giao prompt phase tiếp theo (copy từ mục "PROMPT NGẮN")
  3. FAIL → giao lại CÙNG phase, nêu rõ mục FAIL — không nhảy phase
  4. AI "làm thêm cho tiện" → reject, bảo revert phần ngoài scope phase

================================================================================
LINK
================================================================================

  UpBain web-bot:  .../upbain/tree/cursor/web-bot-schedule-realtime-fe96
  Clender:         .../clender/tree/cursor/fix-web-backup-access-2234
  Spec chi tiết:   docs/research-platform/ (ARCHITECTURE, DECISIONS, DATA_MODEL)

================================================================================
END
================================================================================
