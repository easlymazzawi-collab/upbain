================================================================================
PROMPT DỰ ÁN — UpBain Research Platform v2
Phiên bản: product-first · tham chiếu 2 repo đúng (không copy cả dự án)
================================================================================

!!! QUAN TRỌNG — ĐỌC TRƯỚC KHI CODE !!!

Hai repo dưới đây là NGUỒN THAM CHIẾU duy nhất để rút ưu điểm — KHÔNG phải
“bản để copy sang nguyên xi”. AI phải hiểu spec sản phẩm trước, tự thiết kế
module mới, chỉ mượn Ý TƯỞNG / PATTERN đã proven.

  HAI REPO THAM CHIẾU (ĐÚNG):

  1) Clender
     https://github.com/easlymazzawi-collab/clender
     Branch: cursor/fix-web-backup-access-2234

  2) UpBain — nhánh userbot + web auto (KHÔNG phải nhánh research-bot)
     https://github.com/easlymazzawi-collab/upbain
     Branch: cursor/web-bot-schedule-realtime-fe96

  KHÔNG dùng nhánh cursor/research-bot-platform-fe96 làm “repo mẫu copy”.
  Nhánh research-bot là nơi IMPLEMENT sản phẩm mới — có thể sai, refactor,
  hoặc thay đổi — không phải nguồn sự thật để AI khác paste code.

  CẤM:
  - Copy nguyên file/folder từ 2 repo trên mà không phân tích
  - Merge Clender + UpBain web-bot thành 1 khối lộn xộn
  - Coi nhánh research-bot-platform là “code mẫu”
  - Port Flask clender → FastAPI kiểu 1:1 không redesign
  - Assume mọi thứ trong git cũ = spec cuối cùng

  BẮT BUỘC:
  1. Đọc spec sản phẩm (mục 3–7) trước khi mở repo
  2. Thiết kế kiến trúc mới thống nhất một codebase
  3. Chỉ mượn pattern khi bài toán trùng — viết lại code
  4. Giải thích trong commit/PR: mượn ý gì, khác gì bản gốc

================================================================================
1. HAI REPO — CHỈ ÁP DỤNG ƯU ĐIỂM, KHÔNG COPY CẢ DỰ ÁN
================================================================================

1.1 UpBain · cursor/web-bot-schedule-realtime-fe96
--------------------------------------------------
  ĐÂY LÀ gì:
  Userbot Pyrogram auto-up kênh Telegram + web FastAPI real-time (SSE).
  Đã fix flood, stage batch, lịch auto, topic map, /all task, pin/cursor…

  Mượn ý (nếu phù hợp spec Research Platform):
  - Pipeline userbot: collect batch → xếp sequence → forward kênh RR
  - Web điều khiển: START/STOP, config topic, lịch hằng ngày, SSE log
  - Giảm flood: serial forward, cache message, pipeline lock
  - Auto stage qua Saved Messages
  - /all chạy sau khi hết topic
  - Cấu hình xepbai / mode content-ads
  - Import dữ liệu cũ (.env, topic_map, channels…)

  KHÔNG mang nguyên sang Research Platform:
  - Toàn bộ dashboard v28 như “sản phẩm cuối” — Research Platform là lớp mới
  - Logic pin nguồn nếu spec đã chuyển sang admin notify group
  - Mọi file trong nhánh này coi là nền userbot — không paste cả repo

1.2 Clender · cursor/fix-web-backup-access-2234
------------------------------------------------
  ĐÂY LÀ gì:
  Bot chia sẻ media + deep link + force-join + backup ZIP + web ops.
  KHÁC domain Research Platform (archive theo ngày, userbot, VIP 10 bot…).

  Mượn ý (pattern kỹ thuật — implement lại):
  - Membership gate: cache TTL, fail-open config sai, pending token
  - Delivery robust: copy_messages → forward_messages → từng msg + FloodWait
  - Deep link token số: t.me/bot?start=7754133249527383
  - Backup: SQLite safe copy, ZIP rotate, upload topic Telegram, scheduler
  - Web backup API: GET status / POST run / download

  KHÔNG mang nguyên sang:
  - Bot handlers clender (/share album lẻ, /panel, upload whitelist…)
  - Web Flask + sidebar clender
  - Schema media_albums / media_links (khác archive ngày)
  - Forwarder Telethon clender

1.3 Tóm tắt vai trò
-------------------
  web-bot-schedule-realtime-fe96  →  nền userbot + web ops đã chạy ổn
  fix-web-backup-access-2234      →  pattern bot UX, membership, backup, delivery
  Research Platform mới           →  thiết kế + code thống nhất, chọn lọc 2 bên

================================================================================
2. SẢN PHẨM CẦN XÂY (SPEC — NGUỒN SỰ THẬT)
================================================================================

Tên: Research Platform (mở rộng trên nền UpBain userbot)

Mục tiêu:
  - Archive research theo ngày VN (timezone Asia/Ho_Chi_Minh)
  - 1 userbot up kênh + tối đa 10 bot token delivery cho user
  - Monetize: VIP, Telegram Stars, giftcode
  - Share đua top, catalog/tìm bộ, ads alias có revoke
  - Quản trị web + backup ZIP

Thực thể:
  - 1 userbot (Pyrogram) — kế thừa nền từ web-bot-schedule-realtime-fe96
  - ≤10 bot (aiogram) — delivery end-user
  - 1 SQLite + 1 web backend
  - Forum: nguồn, admin, notify, membership

Archive (A2 — bắt buộc):
  - CHỈ lưu link + metadata trong DB (src_chat_id, src_msg_id, album_msg_ids, seq)
  - KHÔNG forward media vào forum backup
  - User xem: bot copy/forward từ forum nguồn lúc xem

Ads (A1 — bắt buộc):
  - Revoke alias → strip ads lần xem archive SAU
  - KHÔNG xóa tin ads user đã nhận trong chat cũ

Label ngày: 30-06-2026

================================================================================
3. KIẾN TRÚC 3 LỚP (thiết kế sản phẩm)
================================================================================

                    ┌─────────────────────────────┐
                    │ Backend: Web + SQLite +     │
                    │ Orchestrator + Queue        │
                    └──────────────┬──────────────┘
           ┌───────────────────────┼───────────────────────┐
           ▼                       ▼                       ▼
    L1 publish_channels     L2 archive_index        L3 bot_delivery
    Userbot → kênh RR       Ghi link metadata       Bot → user
    (từ web-bot nền)          (không forward backup)   (bot mới/multi)

  ON/OFF từng lớc độc lập trên web.
  Mặc định: kênh trước → bot sau.

  Forum:
    Nguồn     — topic map / bot (ads | plain)
    Admin     — duyệt tên, ZIP backup, contribution
    Notify    — thông báo lượt auto (thay/bổ sung pin nguồn)
    Membership — force-join (pattern clender)

================================================================================
4. QUYẾT ĐỊNH ĐÃ CHỐT
================================================================================

  A1   Không xóa ads chat user; strip khi xem archive sau revoke
  A2   Archive = link + metadata only
  A3   Stars: theo ngày + theo bộ media
  B4   10 nguồn = 10 bot token, queue tuần tự
  B5   Web quản bot, ON/OFF lớp, restart đơn/all
  B6   1 userbot, nhiều admin web
  B7   Hot-swap token, shared DB
  C9   1 bot = 1 topic/ngày (30-06-2026)
  C10  Rollup 30 ngày: mục lục text, không xóa cũ
  C12  1 lượt/ngày
  D15  Gửi tuần tự, delay >=1s
  D16  VIP + giftcode + Stars
  D17  Forward policy; VIP share
  E18  Đóng góp tên forward admin; spam ban 24h→36h
  F23  Ads alias + recheck sau up
  F24  Revoke = inactive, strip khi xem
  F26  1 day nhiều kênh RR
  H34  1 user = 1 click share
  K47  Backup ZIP → topic admin Telegram
  M    Không watermark

================================================================================
5. LUỒNG NGHIỆP VỤ
================================================================================

5.1 Một ngày VN
  Lịch/START → userbot collect (nền web-bot) → xếp seq + ads
  → up kênh RR (L1) → ghi day_items (L2) → notify admin
  → recheck ads → Publish → user /today|/day
  → membership + VIP → delivery tuần tự (L3) → link share

5.2 Delivery (mượn Ý TƯỞNG clender _serve_album — viết hàm mới)
  copy batch → forward batch → từng msg; FloodWait sleep+retry; không skip bài

5.3 Membership (mượn Ý TƯỞNG clender membership.py — viết lại aiogram)
  gate, cache, fail-open, pending deep link, nút Join + Recheck

5.4 VIP / Stars / Giftcode / Share / Catalog / Rollup
  Thiết kế theo spec; không copy file từ repo cũ

5.5 Backup (mượn Ý TƯỞNG clender backup.py)
  safe sqlite copy, ZIP, rotate, scheduler, Telegram upload, web status/run

5.6 Userbot nền (mượn Ý TƯỞNG web-bot-schedule-realtime-fe96)
  Giữ/tích hợp pipeline auto, flood fix, web SSE — hook thêm index sau forward

================================================================================
6. DATA MODEL (thiết kế mới — không schema clender)
================================================================================

  bots, days, day_items, ads_contracts, media_sets,
  users, vip_plans, gift_codes, share_refs, share_clicks,
  user_deliveries, contribution_queue, rollup_indexes

  day_items: seq, src_chat_id, src_msg_id, album_msg_ids[], item_type, ads_alias

================================================================================
7. BOT UX & WEB (yêu cầu chức năng — layout tự thiết kế)
================================================================================

  Bot: /start, /today, /day, /find, /vip, /redeem, /invite, /me, keyboard menu
  Web: config 3 lớp, bots, archive publish/close, VIP, gift, ads, share, users,
       backup status/run/download, membership & forum IDs

  Web userbot nền: giữ khả năng từ web-bot (topic, lịch, START…) — tích hợp,
  không bắt buộc giữ nguyên mọi tab nếu UX mới hợp lý hơn.

================================================================================
8. MAP “ÁP DỤNG ƯU ĐIỂM” (KHÔNG MAP COPY FILE)
================================================================================

  Bài toán                 Lấy ý từ                         Làm mới
  -----------------------  --------------------------------  ------------------
  Auto up + flood          web-bot-schedule-realtime-fe96    Tích hợp + hook index
  Web SSE / lịch / config  web-bot-schedule-realtime-fe96    Mở rộng API platform
  Index ngày sau up        (thiết kế mới)                    Hook sau forward
  Queue 10 bot             (thiết kế mới)                    Orchestrator riêng
  Copy album + FloodWait   clender _serve_album              deliver_item() mới
  Force-join               clender membership              Gate aiogram mới
  Numeric deep link        clender token.py                Util + share ngày
  Backup ZIP + TG          clender backup.py               Scheduler + API FastAPI
  VIP Stars                (thiết kế theo spec)            Không copy clender

================================================================================
9. CÁCH LÀM VIỆC CHO AI
================================================================================

  1. Clone 2 repo tham chiếu (đúng branch ở mục đầu file)
  2. Đọc spec mục 2–7 — thiết kế module
  3. Implement trên codebase đích (có thể nhánh research-bot hoặc branch mới)
  4. Chỉ mở file repo tham chiếu khi stuck 1 bài toán cụ thể
  5. Verify: auto up → index → bot /today → membership → backup

  Clone:
    git clone -b cursor/web-bot-schedule-realtime-fe96 \
      https://github.com/easlymazzawi-collab/upbain /tmp/upbain-webbot-ref

    git clone -b cursor/fix-web-backup-access-2234 \
      https://github.com/easlymazzawi-collab/clender /tmp/clender-ref

================================================================================
10. ANTI-PATTERNS
================================================================================

  x Dùng research-bot-platform-fe96 làm repo mẫu copy
  x Copy cả clender/ hoặc cả tool__tauto_nostage.py sang không review
  x Schema media_albums clender cho archive ngày
  x Forward media vào forum backup (A2)
  x Xóa ads chat user (A1)
  x Folder tên platform/ (crash Windows Pyrogram)
  x Ghép Flask clender + FastAPI không thiết kế lại

================================================================================
11. ACCEPTANCE CRITERIA
================================================================================

  1. Userbot auto up (nền web-bot) + ghi index link
  2. Bot delivery seq đúng, fallback + FloodWait (logic clender)
  3. ≤10 bot queue, restart web
  4. VIP/Stars/giftcode
  5. Ads alias revoke strip (không xóa chat cũ)
  6. Share dedupe + leaderboard
  7. Membership force-join (logic clender)
  8. Backup auto/manual + Telegram
  9. Rollup 30 ngày
  10. Một hệ thống thống nhất — không phải 2 repo dán nhau

================================================================================
12. PROMPT NGẮN CHO AI (copy-paste)
================================================================================

Build UpBain Research Platform v2.

SPEC: archive link+metadata (A2), không xóa ads chat cũ (A1), 3 lớp ON/OFF,
userbot + ≤10 bot, VIP/Stars, ads alias, share, backup.

HAI REPO THAM CHIẾU (chỉ mượn ưu điểm — KHÔNG copy cả dự án):
  1. upbain @ cursor/web-bot-schedule-realtime-fe96
     → userbot auto up, web SSE, flood fix, lịch, topic map
  2. clender @ cursor/fix-web-backup-access-2234
     → membership, delivery fallback, backup ZIP, numeric deep link

KHÔNG dùng research-bot-platform-fe96 làm mẫu paste.

Trước khi code: đọc spec, thiết kế module mới. Stuck thì mượn pattern, viết lại.

================================================================================
13. LINK
================================================================================

  UpBain (tham chiếu userbot/web):
    https://github.com/easlymazzawi-collab/upbain/tree/cursor/web-bot-schedule-realtime-fe96

  Clender (tham chiếu bot/backup/membership):
    https://github.com/easlymazzawi-collab/clender/tree/cursor/fix-web-backup-access-2234

  Docs: docs/research-platform/

================================================================================
END
================================================================================
